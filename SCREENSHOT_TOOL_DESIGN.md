# YanSnap 轻量截图工具设计与实现规格

> 文档状态：已确认  
> 目标平台：Windows 10 22H2、Windows 11，x64  
> 交付形式：单个可直接运行的 EXE  
> 产品名：YanSnap

## 1. 项目目标

开发一款轻量、快速、纯本地的 Windows 截图工具。

用户双击 EXE 后应立即进入截图状态，不需要安装、登录、注册或进行首次配置。工具应覆盖日常截图所需的区域选择、窗口识别、简单标注、复制和保存功能，不发展为包含云服务、录屏、OCR 或插件系统的综合平台。

核心原则：

1. 点击即用，默认设置即可完成全部基础操作。
2. 单文件发布，不依赖第三方运行时或附带 DLL。
3. 本地处理，不联网、不上传、不采集遥测数据。
4. 优先保证启动速度、截图准确性和多显示器兼容性。
5. 界面保持克制，只呈现截图过程真正需要的功能。
6. 第一版不为未来功能预先引入大型框架或复杂抽象。

## 2. 参考项目与借鉴范围

### 2.1 ShareX

参考项目：[ShareX](https://github.com/ShareX/ShareX)

借鉴内容：

- 快捷键触发截图。
- 区域、窗口、显示器等捕获方式。
- 截图后复制、保存等明确的动作。
- 标注工具的基本类型。

不采用：

- 上传服务和分享工作流。
- OCR、二维码、录屏、GIF、图像工具箱。
- 插件和自定义任务系统。
- 复杂的历史记录与自动化体系。

### 2.2 Flameshot

参考项目：[Flameshot](https://github.com/flameshot-org/flameshot)

借鉴内容：

- 在截图遮罩层内直接标注。
- 工具栏跟随选区显示。
- 通过键盘快速完成复制、保存和取消。

### 2.3 Greenshot

参考项目：[Greenshot](https://github.com/greenshot/greenshot)

借鉴内容：

- 区域、窗口、全屏三个核心捕获场景。
- 简单、清晰的输出方式。
- 面向日常办公和问题反馈的标注能力。

### 2.4 开源许可边界

ShareX、Flameshot 和 Greenshot 的主要代码采用 GPL 系列许可证。本项目只参考公开的产品行为和交互模式，不复制其源代码、图标、素材或其他受版权保护的实现。

项目代码建议采用 MIT License。程序图标和工具栏图标应自行绘制；如果使用第三方素材，必须选择允许分发的许可证，并在仓库中保留许可证和署名信息。

## 3. 技术方案

### 3.1 推荐技术栈

- 语言：C++20
- 窗口系统：Win32 API
- 绘制：Direct2D
- 文字：DirectWrite
- 屏幕捕获：Win32 GDI，使用 `BitBlt` 和 `CAPTUREBLT`
- 图像编码：Windows Imaging Component（WIC）
- 剪贴板：Win32 Clipboard API，优先提供 `CF_DIBV5`
- 构建系统：CMake
- 编译器：MSVC
- 目标架构：x64
- 运行库：静态链接 MSVC Runtime（`/MT`）
- DPI 模式：Per-Monitor DPI Aware V2
- 字符集：Unicode

Windows 官方参考：

- [Capturing an Image](https://learn.microsoft.com/en-us/windows/win32/gdi/capturing-an-image)
- [Windows Imaging Component Overview](https://learn.microsoft.com/en-us/windows/win32/wic/-wic-about-windows-imaging-codec)

### 3.2 选择原生 C++ 的原因

- 不需要携带 Chromium、Qt 或 .NET 运行时。
- 可以输出体积较小、启动快速的单文件 EXE。
- Win32 可直接处理全局快捷键、系统托盘、剪贴板、窗口识别和多显示器信息。
- Direct2D 和 DirectWrite 足够完成截图遮罩、标注工具和中文文字绘制。
- WIC 是 Windows 自带组件，可完成 PNG 编码。

### 3.3 不采用的技术路线

| 技术路线 | 不采用原因 |
| --- | --- |
| Electron | 安装包、运行内存和启动成本明显超过本项目需求 |
| Qt | 需要携带较多运行组件，第一版不需要跨平台 |
| WPF/WinForms | 开发较快，但自包含发布体积更大，并依赖 .NET 运行环境或附带运行时 |
| Rust GUI 框架 | Win32 封装和 GUI 生态会增加第一版实现复杂度，没有带来明确产品收益 |

## 4. 产品行为

### 4.1 启动与单实例

1. 用户首次双击 `YanSnap.exe`。
2. 程序创建托盘实例，在右下角提示“YanSnap 已在后台运行”，不立即进入截图模式。
3. 鼠标移入托盘图标时显示产品名、当前截图快捷键和 `F3` 贴图快捷键，用户通过全局快捷键或托盘菜单开始截图。
4. 用户再次双击 EXE 时，不创建第二个实例、不开始截图，只通知已有实例再次显示后台运行提示。
5. 单实例通信建议使用命名互斥量加命名管道，或互斥量加注册窗口消息。
6. 托盘菜单必须提供“退出”，退出后不保留后台进程。

程序不应默认设置开机启动。后续如果加入开机启动选项，必须由用户主动开启。

### 4.2 默认快捷键

默认全局快捷键：`F1`

默认使用单个功能键以便快速触发，同时允许用户设置其他单键或组合键。

要求：

- 使用 `RegisterHotKey` 注册。
- 快捷键可在设置中更改，允许无修饰键的单键。
- 如果快捷键注册失败，程序仍应正常运行。
- 注册失败时通过托盘通知或设置页提示用户更换快捷键。
- 退出程序时释放快捷键。

### 4.3 截图流程

1. 用户点击托盘“截图”或按全局快捷键。
2. 程序先获取虚拟桌面的静态截图。
3. 截图完成后再显示遮罩窗口，避免工具自身出现在截图中。
4. 鼠标移动时自动识别光标下的可见顶层窗口并高亮。
5. 用户单击可直接选择高亮窗口。
6. 用户按住鼠标拖动时进入自由区域选择。
7. 选区建立后允许移动选区或拖动八个控制点调整大小。
8. 选区附近显示标注工具栏。
9. 用户可以直接完成，也可以添加标注。
10. 完成后将选区与标注合成为最终图像。
11. 根据用户动作复制到剪贴板或保存到文件。
12. 遮罩关闭，程序返回托盘状态。

### 4.4 鼠标操作

| 操作 | 行为 |
| --- | --- |
| 鼠标移动，尚未拖动 | 自动高亮光标下的窗口 |
| 单击高亮窗口 | 选择该窗口 |
| 按住左键拖动 | 创建自由选区 |
| 拖动选区内部 | 移动选区 |
| 拖动控制点 | 调整选区大小 |
| 双击选区 | 复制截图并完成 |
| 右键 | 取消当前标注；没有标注时取消截图 |

### 4.5 键盘操作

| 快捷键 | 行为 |
| --- | --- |
| `Enter` | 复制到剪贴板并完成 |
| `Ctrl + C` | 复制到剪贴板并完成 |
| `Ctrl + S` | 打开保存对话框 |
| `Esc` | 先取消当前编辑状态，再取消截图 |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Y` | 重做 |
| 方向键 | 选区移动 1 个物理像素 |
| `Shift + 方向键` | 选区移动 10 个物理像素 |
| `Delete` | 删除当前选中的标注对象 |

第一版不强制为每个标注工具设置单键快捷键，以避免快捷键逻辑干扰中文文字输入。

## 5. 截图界面

### 5.1 遮罩层

- 遮罩覆盖完整虚拟桌面。
- 未选区域显示约 45% 的黑色蒙层。
- 选区显示原始截图亮度。
- 选区边框使用清晰的蓝色细线。
- 选区提供八个调整控制点。
- 鼠标为十字准星，进入工具栏或文字输入状态时切换为对应光标。

为了正确处理多显示器混合 DPI，推荐为每台显示器创建一个顶层无边框遮罩窗口，由同一个控制器统一管理虚拟桌面坐标、选区和标注状态。

选区允许跨越显示器。所有内部几何计算使用物理像素坐标，界面尺寸再根据当前显示器 DPI 换算。

### 5.2 窗口识别

- 使用 `WindowFromPoint` 获取光标所在窗口。
- 过滤当前程序的遮罩窗口、隐藏窗口、透明穿透窗口和桌面内部辅助窗口。
- 向上查找合适的顶层窗口。
- 优先使用 `DwmGetWindowAttribute` 的扩展窗口边界，减少阴影和不可见边框误差。
- 无法识别有效窗口时，不显示窗口高亮，用户仍可自由拖动选区。

### 5.3 尺寸标签与放大镜

选区创建和调整过程中显示：

- 当前宽度和高度，例如 `1280 × 720`。
- 光标附近的像素放大镜。
- 放大镜中心显示准星。
- 放大镜应自动避开屏幕边缘和当前选区操作点。

为控制范围，第一版放大镜只显示画面，不提供颜色拾取和 RGB/HEX 信息。

### 5.4 浮动工具栏

工具栏默认放在选区底部外侧，空间不足时放到顶部或选区内部。

建议顺序：

1. 矩形
2. 箭头
3. 画笔
4. 文字
5. 马赛克
6. 撤销
7. 重做
8. 保存
9. 复制并完成
10. 取消

工具栏要求：

- 深色半透明背景。
- 圆角外观。
- 点击区域不得小于 32 × 32 逻辑像素。
- 当前工具有明确选中状态。
- 图标旁可通过 Tooltip 显示中文名称和快捷键。
- 自动避开虚拟桌面边缘。

不需要单独打开完整图片编辑器。

## 6. 标注系统

### 6.1 数据模型

标注在编辑期间保持为矢量对象，不直接修改原始截图。完成截图时再按创建顺序渲染到输出位图。

建议基础接口：

```cpp
class Annotation {
public:
    virtual ~Annotation() = default;
    virtual void Draw(RenderContext& context) const = 0;
    virtual RectF Bounds() const = 0;
    virtual bool HitTest(PointF point) const = 0;
};
```

每个对象至少保存：

- 类型
- 起点与终点，或路径点集合
- 颜色
- 线宽
- 创建顺序
- 工具特有参数

### 6.2 矩形

- 默认红色描边。
- 默认不填充。
- 支持拖动创建。
- 第一版可以只提供三档线宽，不必实现连续滑块。

### 6.3 箭头

- 默认红色。
- 箭头头部随线宽缩放。
- 短箭头也应保持可辨认。
- 输出时避免箭头超出截图边界导致裁切异常。

### 6.4 画笔

- 记录鼠标轨迹点。
- 绘制时进行基础平滑处理。
- 鼠标移动事件过密时可按距离阈值合并点。
- 不需要压感支持。

### 6.5 文字

- 点击后创建内联文字输入框。
- 支持中文输入法、退格、换行和粘贴。
- 默认字体使用系统 UI 字体。
- 默认颜色为红色，文字提供浅色或深色轮廓，确保在复杂背景上可读。
- 截图切换或取消时必须正确结束 IME 组合输入。

### 6.6 马赛克

- 用户拖动创建矩形马赛克区域。
- 合成输出时对区域进行像素块采样。
- 马赛克必须实际破坏细节，不能只在界面上叠加半透明效果。
- 第一版使用固定或三档像素块大小。

### 6.7 撤销与重做

- 使用命令栈或标注对象栈。
- 创建、删除、移动和修改标注都应可撤销。
- 新操作发生后清空重做栈。
- 截图结束后释放全部编辑状态，不保存编辑历史。

## 7. 输出

### 7.1 剪贴板

- 默认完成动作是复制图像到剪贴板。
- 优先提供带 Alpha 信息的 `CF_DIBV5`。
- 可同时提供兼容性更好的位图格式。
- 剪贴板被其他程序短暂占用时，进行少量、短间隔重试。
- 多次失败时保留当前截图状态并提示失败，不能静默丢失截图。

### 7.2 文件保存

- 第一版必须支持 PNG。
- 使用 WIC 编码。
- 保存对话框初始目录默认为系统“图片”目录下的 `Screenshots` 文件夹。
- 自动文件名格式：

```text
Screenshot_yyyyMMdd_HHmmss.png
```

- 同一秒出现重名时追加序号：

```text
Screenshot_20260728_153015_2.png
```

- 正确处理中文、空格和较长路径。
- 写入失败时不得关闭截图界面，应提示错误并允许用户重新选择位置。

### 7.3 成功提示

- 复制成功或保存成功后可以显示简短托盘通知。
- 通知不得获取键盘焦点。
- 通知内容保持简短，例如“截图已复制，1280 × 720”。
- 设置中允许关闭成功通知。

## 8. 托盘与设置

### 8.1 托盘菜单

第一版菜单：

- 截图
- 打开截图文件夹
- 设置
- 关于
- 退出

双击托盘图标直接开始截图。

### 8.2 设置项

第一版设置页只包含：

- 全局截图快捷键
- 默认完成动作：仅复制 / 复制并自动保存
- 默认保存目录
- 是否显示成功通知
- 是否在截图中包含鼠标指针，默认关闭

暂不提供：

- 主题商城
- 账号
- 云服务
- 自动更新
- 代理和网络设置
- 插件管理

### 8.3 设置存储

- 默认保存到 `%LocalAppData%\YanSnap\settings.ini`。
- 使用 INI 或项目自有的极简文本配置，避免为 JSON 引入大型依赖。
- 配置损坏时回退到默认值并保留一份损坏文件用于排查。
- 设置写入应采用临时文件加替换的方式，避免程序异常退出造成半写入文件。
- 不在用户未授权时修改注册表。

## 9. 推荐代码结构

```text
YanSnap/
├─ CMakeLists.txt
├─ LICENSE
├─ README.md
├─ docs/
│  └─ manual-test-checklist.md
├─ resources/
│  ├─ app.ico
│  └─ YanSnap.rc
├─ src/
│  ├─ app/
│  │  ├─ App.cpp
│  │  ├─ SingleInstance.cpp
│  │  ├─ HotkeyManager.cpp
│  │  └─ TrayIcon.cpp
│  ├─ capture/
│  │  ├─ DesktopCapture.cpp
│  │  ├─ MonitorLayout.cpp
│  │  └─ WindowDetector.cpp
│  ├─ overlay/
│  │  ├─ OverlayController.cpp
│  │  ├─ OverlayWindow.cpp
│  │  ├─ SelectionModel.cpp
│  │  ├─ Magnifier.cpp
│  │  └─ Toolbar.cpp
│  ├─ annotation/
│  │  ├─ Annotation.h
│  │  ├─ RectangleAnnotation.cpp
│  │  ├─ ArrowAnnotation.cpp
│  │  ├─ PenAnnotation.cpp
│  │  ├─ TextAnnotation.cpp
│  │  ├─ MosaicAnnotation.cpp
│  │  └─ UndoStack.cpp
│  ├─ export/
│  │  ├─ ClipboardExporter.cpp
│  │  ├─ ImageComposer.cpp
│  │  └─ PngEncoder.cpp
│  ├─ settings/
│  │  ├─ Settings.cpp
│  │  └─ SettingsWindow.cpp
│  └─ common/
│     ├─ ComPtr.h
│     ├─ Geometry.h
│     ├─ ScopeGuard.h
│     └─ Win32Error.cpp
└─ tests/
   ├─ GeometryTests.cpp
   ├─ SelectionTests.cpp
   └─ SettingsTests.cpp
```

这只是推荐边界。实现时可以合并过小文件，但不应把屏幕捕获、遮罩交互、标注渲染和文件输出全部堆入一个窗口过程。

## 10. 资源和生命周期要求

- GDI 对象、HDC、HBITMAP、窗口句柄和 COM 对象必须使用 RAII 管理。
- 每次截图完成或取消后释放完整桌面位图和遮罩资源。
- 托盘空闲状态不应保留上一次全屏截图。
- 处理显示器接入、移除、分辨率变化和 DPI 变化消息。
- 捕获期间发生显示配置变化时，安全取消本次截图并允许重新开始。
- 不允许在 UI 线程执行明显耗时的 PNG 写入；大尺寸图像编码可放入工作线程。
- 工作线程不能直接操作窗口控件，应通过消息返回结果。
- 程序退出时清理托盘图标、快捷键、IPC 对象和后台线程。

## 11. 性能目标

以下为 Release x64 构建的目标值，实施时应记录实际测试结果：

| 指标 | 目标 |
| --- | --- |
| EXE 体积 | 小于 5 MB |
| 冷启动至出现截图遮罩 | 小于 200 ms |
| 托盘空闲工作集 | 小于 25 MB |
| 普通双屏截图内存峰值 | 在合理范围内，截图完成后明显回落 |
| 遮罩与拖选响应 | 目标接近 60 FPS，无明显拖影 |
| 外部运行依赖 | 无第三方 DLL，无额外运行时安装 |
| 网络请求 | 0 |

测试性能时至少记录：

- 单个 1080p 显示器。
- 单个 4K 显示器。
- 1080p 与 4K 混合双屏。
- 100% 与 150% 混合 DPI。

如果 EXE 体积与可维护性冲突，优先保证功能正确和不携带第三方运行时，5 MB 作为优化目标而不是阻塞发布的硬性条件。

## 12. 兼容性与已知边界

第一版支持：

- Windows 10 22H2 x64。
- Windows 11 x64。
- 100%、125%、150%、200% DPI。
- 横屏、竖屏和多显示器。
- 显示器位于主显示器左侧或上方产生的负坐标。
- 中文用户名、中文文件名和中文保存路径。

已知边界：

- UAC 安全桌面不能由普通权限程序截图。
- 受 DRM 或系统保护的视频内容可能显示为黑色。
- 某些独占全屏游戏可能无法通过普通桌面捕获获得正确图像。
- HDR 桌面使用 GDI 捕获时可能产生色彩或亮度偏差。
- 第一版不申请管理员权限，也不通过注入方式绕过系统限制。

若后续用户对 HDR、独占全屏游戏或高帧率录屏有明确需求，再评估 Windows Graphics Capture 或 Desktop Duplication API，不在第一版同时维护多套捕获后端。

## 13. 隐私与安全

- 程序不包含网络客户端逻辑。
- 不上传截图、文件路径、快捷键或使用数据。
- 不记录截图内容。
- 不保存截图历史数据库。
- 不创建用户账号或设备标识。
- 错误日志不得包含截图像素数据。
- 日志中的文件路径应尽量减少或允许关闭。
- 截图仅在用户明确执行复制或保存动作后离开进程内存。

## 14. 开发阶段

### 阶段 A：可运行骨架

交付：

- CMake 工程。
- Win32 程序入口。
- 单实例。
- 托盘图标与退出菜单。
- 全局快捷键。
- Release x64 构建脚本或明确构建命令。

通过条件：

- 双击 EXE 可以运行。
- 重复双击不会出现多个实例。
- 快捷键和托盘菜单可以触发占位截图动作。
- 退出后没有残留进程。

### 阶段 B：核心截图

交付：

- 虚拟桌面截图。
- 多显示器遮罩。
- 自由框选。
- 窗口识别。
- 选区移动和缩放。
- 复制到剪贴板。
- 保存 PNG。

通过条件：

- 在单屏和混合 DPI 双屏环境中选择区域不会出现明显坐标偏移。
- 复制出的图片尺寸与显示的选区尺寸一致。
- 保存结果与剪贴板结果一致。
- 取消截图不会残留遮罩窗口或大块内存。

### 阶段 C：标注体验

交付：

- 浮动工具栏。
- 矩形、箭头、画笔、文字和马赛克。
- 撤销与重做。
- 放大镜和尺寸标签。

通过条件：

- 标注预览与最终输出位置一致。
- 中文文字可通过常见中文输入法正常输入。
- 马赛克输出不可轻易还原原始细节。
- 选区位于屏幕边缘时，工具栏和放大镜仍可见。

### 阶段 D：设置、健壮性和发布

交付：

- 设置页。
- 快捷键修改。
- 默认保存路径和完成动作设置。
- 错误提示。
- 图标、版本信息、许可证和 README。
- 自动化测试与人工测试清单。
- 最终单文件 Release EXE。

通过条件：

- 配置损坏不会导致程序无法启动。
- 快捷键冲突有清晰提示。
- 保存或剪贴板失败不会丢失正在编辑的截图。
- Windows 10 和 Windows 11 的测试清单完成。

## 15. 验收清单

### 15.1 基础功能

- [ ] 双击 EXE 立即进入截图。
- [ ] 第二次启动触发已有实例截图。
- [ ] 托盘菜单可以截图和退出。
- [ ] 全局快捷键可以截图。
- [ ] 可以自由拖动选择区域。
- [ ] 可以单击选择窗口。
- [ ] 可以调整和移动选区。
- [ ] 可以复制截图。
- [ ] 可以保存 PNG。
- [ ] 文件名不会覆盖已有文件。
- [ ] 可以取消截图。

### 15.2 标注

- [ ] 矩形显示和输出位置一致。
- [ ] 箭头方向正确。
- [ ] 画笔轨迹连续。
- [ ] 中文文字输入正常。
- [ ] 马赛克实际作用于输出图像。
- [ ] 撤销与重做顺序正确。
- [ ] 删除对象可撤销。

### 15.3 多显示器与 DPI

- [ ] 主屏 100%、副屏 150% 时坐标准确。
- [ ] 副屏位于主屏左侧时可以截图。
- [ ] 副屏位于主屏上方时可以截图。
- [ ] 选区可以跨显示器。
- [ ] 在 4K 屏幕上工具栏大小合理。
- [ ] 显示配置改变后程序不会崩溃。

### 15.4 错误场景

- [ ] 快捷键被占用时程序仍可使用。
- [ ] 剪贴板被占用时进行重试并给出反馈。
- [ ] 保存目录无写入权限时截图界面不会直接关闭。
- [ ] 配置文件损坏时回退默认配置。
- [ ] 取消大尺寸截图后内存可以释放。
- [ ] 退出时无残留进程。

### 15.5 隐私

- [ ] 正常运行过程中没有网络请求。
- [ ] 没有账号、登录或设备标识逻辑。
- [ ] 日志不包含截图内容。
- [ ] 不自动上传或建立截图历史数据库。

## 16. 第一版完成定义

满足以下条件才算第一版完成：

1. 阶段 A 至 D 的交付物全部存在。
2. 核心验收清单全部通过。
3. 多显示器和 DPI 测试不存在阻塞使用的问题。
4. 输出为可直接运行的 x64 单文件 EXE。
5. 新的 Windows 用户无需安装开发工具或运行时。
6. 仓库包含构建说明、使用说明、快捷键说明和已知限制。
7. 没有登录、联网、上传、遥测和自动更新功能。
8. Release 构建不携带未使用的大型第三方依赖。
9. Codex 在交付前实际运行程序，并至少完成一次区域截图、一次窗口截图、一次复制和一次保存测试。

## 17. 明确排除项

以下功能不属于第一版，除非产品需求再次确认：

- 账号、注册、登录。
- 云同步和团队空间。
- 图片上传和短链接。
- OCR、翻译和二维码识别。
- 录屏、GIF 和摄像头。
- 滚动长截图。
- 贴图。
- 截图历史数据库和全文搜索。
- 插件系统。
- 脚本和自动化工作流。
- 自动更新。
- 多语言系统；第一版仅需中文界面。
- Windows on ARM。
- macOS 和 Linux。

## 18. Codex 实施约束

后续使用 Codex 实现时，应遵守：

1. 先完成阶段 A 并验证，再进入阶段 B，不要一次性生成全部代码。
2. 每个阶段都必须提供可运行构建，不允许长期停留在不可编译状态。
3. 优先使用 Windows 系统组件，不随意增加第三方依赖。
4. 新增第三方依赖前必须说明用途、体积、许可证和不可替代原因。
5. 所有 Win32、GDI 和 COM 资源必须使用明确的生命周期管理。
6. 不以关闭 DPI 感知来规避坐标问题。
7. 不以管理员权限解决普通截图问题。
8. 不复制参考项目的 GPL 源代码或素材。
9. 每完成一个阶段，更新 README 和人工测试清单。
10. 最终交付必须包含实际构建的 EXE，而不仅是源代码。
