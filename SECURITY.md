# 安全策略

## 支持范围

安全修复优先应用于最新 GitHub Release。旧版本可能不会单独维护，请在报告前确认问题是否能在最新版本复现。

| 版本 | 状态 |
| --- | --- |
| `1.x` 最新版本 | 支持 |
| 更早版本 | 不保证 |

## 报告漏洞

请不要在公开 Issue 中披露可被利用的漏洞、令牌、私人截图或其他敏感信息。

优先使用仓库 **Security** 页面中的私密漏洞报告功能。报告中请包含：

- 受影响版本
- Windows 版本和系统架构
- 可重复的操作步骤
- 潜在影响
- 建议修复方式（如有）

普通缺陷、崩溃和兼容性问题可以通过公开 Issue 报告，但请先删除个人路径、剪贴板内容和截图中的敏感信息。

## 安全边界

YanSnap 按以下原则设计：

- 截图、文字识别和图像编码在本机完成
- OCR 使用 Windows 本地语言组件，不上传截图或识别结果
- 不包含账号、遥测、广告、云上传或自动更新
- 不请求管理员权限
- 不尝试捕获 UAC 安全桌面或绕过 DRM
- 仅在用户主动启用时写入当前用户的 Windows 登录启动项
- 便携配置默认保存在程序目录的 `config.ini`

未签名的 Release 可能触发 Windows SmartScreen“未知发布者”提示。该提示不等同于恶意软件检测；用户仍应只从官方 Releases 下载并验证 SHA-256。
